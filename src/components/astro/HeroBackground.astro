<div class="absolute inset-0 overflow-hidden pointer-events-none -z-10">
  <!-- Blob 1 -->
  <div 
    class="blob blob-1 absolute top-0 left-1/4 w-96 h-96 bg-accent/20 rounded-full"
  ></div>
  
  <!-- Blob 2 -->
  <div 
    class="blob blob-2 absolute top-0 right-1/4 w-96 h-96 bg-purple-500/20 rounded-full"
  ></div>
  
  <!-- Blob 3 -->
  <div 
    class="blob blob-3 absolute -bottom-32 left-1/3 w-96 h-96 bg-pink-500/20 rounded-full"
  ></div>
</div>

<style>
  @keyframes blob {
    0% {
      transform: translate(0px, 0px) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
    100% {
      transform: translate(0px, 0px) scale(1);
    }
  }

  @keyframes blob-entrance {
    from {
      opacity: 0;
      transform: scale(0.3);
    }
    to {
      opacity: 0.7;
      transform: scale(1);
    }
  }

  /* Desktop: Full effects */
  @media (min-width: 769px) {
    .blob {
      filter: blur(96px);
      opacity: 0;
      mix-blend-mode: multiply;
      animation: blob-entrance 1.2s cubic-bezier(0.22, 1, 0.36, 1) forwards,
                 blob 7s 1.2s infinite;
      will-change: transform;
    }

    .blob-2 {
      animation: blob-entrance 1.2s 0.3s cubic-bezier(0.22, 1, 0.36, 1) forwards,
                 blob 7s 1.5s infinite;
    }

    .blob-3 {
      animation: blob-entrance 1.2s 0.6s cubic-bezier(0.22, 1, 0.36, 1) forwards,
                 blob 7s 1.8s infinite;
    }
  }

  /* Mobile: Reduced effects for performance */
  @media (max-width: 768px) {
    .blob {
      filter: blur(48px); /* blur-xl equivalent - much lighter */
      opacity: 0.4; /* Reduced opacity */
      /* mix-blend-mode removed for better performance */
      animation: none; /* Disable animation on mobile */
      transform: scale(0.8); /* Smaller blobs on mobile */
    }
  }

  /* Respect reduced motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .blob {
      animation: none !important;
    }
  }
</style>
